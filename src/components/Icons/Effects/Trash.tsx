import UseAnimations from 'react-useanimations';
import trash from 'react-useanimations/lib/trash';
export const TrashIcon = () => {
  return <UseAnimations strokeColor={'#fff'} animation={trash} size={20} />;
};
